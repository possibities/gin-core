run:
  timeout: 5m
linters:
  enable:
    - bodyclose
    - contextcheck
    - errcheck
    - gofmt
    - goimports
    - gosimple
    - govet
    - revive
    - staticcheck
    - unused
    - gosec
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
